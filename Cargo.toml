[workspace]
members = [
    "bench",
    "client",
    "common",
    "exhauster",
    "proto",
    "rudder",
    "storage",
    "tests",
    "wheel",
]

[profile.bench]
debug = true

[profile.release]
debug = true
