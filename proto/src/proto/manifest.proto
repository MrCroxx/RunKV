syntax = "proto3";

package manifest;

enum SSTableOP {
  INSERT = 0;
  DELETE = 1;
}

message SSTableDiff {
  uint64 id = 1;
  SSTableOP op = 2;
}

message Level {
  repeated SSTableDiff sstable_diffs = 1;
}

message VersionDiff {
  uint64 id = 1;
  repeated Level levels = 2;
}
